import { config } from 'dotenv'

const {resolve} = require('path')

module.exports ={
    config: resolve(__dirname, 'src', 'configs', 'db.js'),
    'models-path': resolve(__dirname, 'src', 'apps', 'migrations')
}